<!DOCTYPE html>
<html>
<head>
    <title>VueForm</title>
</head>
<body>
    <h1>VueForm</h1>
    @*Vue实现Form提交对象到后台*@
    <div id="app">
        <form asp-action="/vueform" method="post">
            <div>
                <span>Url:</span>
                <input type="text" name="blog.url" />
            </div>
            <br />

            <div v-for="(post,index) in count">
                <p>{{index}}</p>
                <span>Title:</span>
                @*Vue 属性设置使用字符串并接方式*@
                <input type="text" :name="'blog.posts['+ index + '].title'" />
                <span>content:</span>
                <input type="text" :name="'blog.posts['+ index + '].content'" />
            </div>
            <button type="button" @@click="addPost">添加一组</button>
            <button type="submit">提交保存</button>
        </form>
    </div>
</body>
</html>
<script type="text/javascript" src="https://unpkg.com/vue@2.2.6/dist/vue.min.js"></script>

<script type="text/javascript">
    var vuepost = {
        data() {
            return {
                count:[1]
            }
        },
        methods: {
            addPost() {
                console.log('添加一组数据')
                this.count.push(1)
            },
          
        }
    }
    var Crot = Vue.extend(vuepost)
    new Crot().$mount('#app')
</script>
